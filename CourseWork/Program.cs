using comp_lab.Labs.glsl;
using comp_lab.Labs.lab2.structs;
//
// var lexer = new GlslLexer();
// string source = File.ReadAllText("C:\\Work\\Repos\\comp-lab1\\CourseWork\\TestData\\CompileMe.glsl");
// List<GrammarPart> tokens = lexer.Tokenize(source);
//
// foreach (Term t in tokens.Cast<Term>())
// {
//     Console.WriteLine($"{t.Name} : {t.Kind}");
// }
//
// var parser = new GlslSyntaxAnalyzer();
// bool ok = parser.Analyze(tokens);
// if (ok) Console.WriteLine("Синтаксис верен");